export interface Products {
    image: string,
    rating: number,
    volume: number,
    price: number
}

interface Customer {

}


/*
* 	name VARCHAR(225) PRIMARY KEY,
	image TEXT,
	rating DECIMAL(1,1),
	volume INTEGER,
	price DECIMAL(2, 2)

    id BIGSERIAL PRIMARY KEY,
	name VARCHAR(225),
	review VARCHAR(225),
	rating DECIMAL(1,1)	
    
*/